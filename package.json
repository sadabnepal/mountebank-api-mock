{
  "name": "mountebank-service-virtualization",
  "description": "test service using mocha and supertest.",
  "version": "0.0.1",
  "license": "UNLICENSED",
  "scripts": {
    "start": "mb --configfile imposters.ejs --allowInjection --loglevel info"
  },
  "devDependencies": {
    "@mbtest/mountebank": "^2.9.3"
  }
}
